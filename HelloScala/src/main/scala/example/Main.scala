package example

object Main extends App {
val seq = Seq(1,2,5,2,3);
  println(s"The largest number in the sequent is ${seq.max}")

  println(16.toHexString)

  println((0 until 10).contains(10))

  println("bar".take(2))
}
